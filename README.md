# Plexo

App de operaciones cripto USDT/ARS con auto-sync a Google Drive.

## Setup
1. `npm install`
2. Crear `.env.local` con `VITE_GOOGLE_CLIENT_ID=tu_client_id`
3. `npm run dev`

## Deploy
Push a GitHub, conectar con Vercel, configurar env var `VITE_GOOGLE_CLIENT_ID`.
